# JD 电商数据可视化平台 · 使用说明

> 面向运营 / 数据同学的项目使用文档。技术同学请同时参考代码注释（尤其是 `frontend/src/metrics.ts`、`backend/jobs/transforms.py`）。

---

## 1. 项目简介

把多个店铺的「商品明细表（xlsx）」「推广数据表（csv）」「退款数据」整合清洗后，
以 **可交互的可视化页面** 呈现：模块总览、单 SPU 指标卡、趋势、工作日/节假日对比、综合分析等。

- 当前模块：`pop_spu_detail`（POP 商品 SPU 明细），页面路由名为 `pop_spu_detail`。
- 数据区间示例：`2026-08-14 ~ 2026-09-17`（以数据源实际产出为准，见页面顶部「数据区间」）。

---

## 2. 技术架构

| 层 | 技术 | 说明 |
| --- | --- | --- |
| 批处理 | Python + Polars + Pydantic | `backend/jobs`：读源表 → 清洗 → 产出层级 JSON（店铺→SPU→日期→指标）、汇总 JSON |
| 接口 | FastAPI | `backend/app`：manifest / 模块分页明细 / 单品分析 / 状态 / 手动刷新 |
| 前端 | Vue 3 + Vite + TypeScript | `frontend`：SPA，模块页 + 单品分析页 |
| 部署 | 单端口 8000 | 生产模式一个 uvicorn 同时托管 API + 页面 + SPU 图片 |

**生产模式要点**

- 前端 `npm run build` 产物在 `frontend/dist`，由 FastAPI 的 `StaticFiles` **按请求实时读取磁盘**，
  因此**只改前端**：重新 `build` 即可，无需重启服务。
- **改后端代码或重跑批处理（数据源变更）**：必须重跑 `backend/jobs/run_batch.py` 并**重启 uvicorn 8000**，
  否则接口仍返回旧字段/旧数据（表现为「改了没生效」）。

---

## 3. 数据来源与目录

- 数据源根目录：`ResourceData/`
  - `SPU商品名称映射表.xlsx`：SPU → 名称映射（缺失时降级为原始 SPU 号，不中断）。
  - `单品spu图片/`：SPU 图片。
  - `数据源表目录/`：各店铺明细 xlsx / 推广 csv（固定文件夹，批处理不再遍历「最新日期文件夹」）。
- 批处理产出：`backend/app/data/`（manifest.json、各模块 `*.json` / `*.summary.json`、status.json、images/ + thumbs/）。

**指标口径（重要）**

- 可累加原始指标（访客/客户/单量/件数/金额/搜索曝光与点击/推广花费与成交/退款）直接求和。
- 派生比率一律 **先按分子分母各自求和，再相除**，保证「单日 / 店铺汇总 / 日期汇总 / 多日区间」四个层级口径一致：
  - 成交转化率 = 成交客户数 / 商品访客数
  - 客单价 = 成交金额 / 成交客户数
  - 搜索点击率 = 搜索点击次数 / 搜索曝光次数
  - ROI = 推广成交金额 / 推广花费
  - 推广占比 = 推广花费 / 成交金额

---

## 4. 本地启动

```powershell
# 1) 重跑批处理（数据源有更新时；首次也必须跑）
#    在「项目根目录」（即包含 .venv 的目录，可任意改名/移动）下执行：
.venv\Scripts\python.exe -m backend.jobs.run_batch

# 2) 启动服务（单端口 8000，同时提供 API + 页面 + 图片）
.venv\Scripts\python.exe -m uvicorn backend.app.main:app --app-dir "." --host 127.0.0.1 --port 8000

# 3) 仅改了前端时，另开终端重新构建（无需重启服务）
cd frontend
npm run build
```

- 页面访问：<http://127.0.0.1:8000/>
- 前端本地开发（热更新）：`npm run dev`（默认 5173）。

---

## 5. 页面说明

### 5.1 模块页 `/module/:moduleId`（路由名 `pop_spu_detail`）

**顶部工具条（保持不变的一行）**

- **统计区间**：范围选择器（点开日历，连续点「起点 → 终点」自动应用）。
  - 内置 **当月 / 上月** 快捷按钮：直接把区间设为对应月份，并与数据可用区间取交集；
    月份内日期不全时「有多少天算多少天」，整月落在数据区间外时退化到最近端点。
  - 另有 **全区间** 一键还原（清空选择 = 展示全部日期）。
- 搜索框：按 SPU 号 / 商品名称模糊搜索。
- 数据区间状态、上次批处理更新时间。
- **⚙ 指标配置**：勾选要在卡片/矩阵中展示的指标（见 5.3）。
- **手动刷新**：触发后端重跑批处理并清缓存。

**两种展示形态（由所选区间自动切换）**

1. **单日模式（默认，区间起点=终点，即最新一天）**
   - 一个 SPU 一张**矩形单元卡**，以网格排列（自适应多列）。
   - 卡片顶部：商品信息（名称 / SPU / 类目 / 所属店铺）+「单品分析 →」入口。
   - 卡片左侧：商品图片（180×180，与旧表格图片同尺寸）。
   - 卡片右侧：**勾选的指标，一个指标一行**（由指标配置控制）。

2. **区间模式（起点≠终点，即选择了日期范围）**
   - 每个 SPU **单独占一整行**（这就是「单独占一行」的目的：右侧预留横向空间）。
   - 行左侧（固定）：商品图片（120×120）+ 商品信息 + 单品分析入口。
   - 行右侧：**「指标 × 日期」矩阵**——
     - 行 = 勾选的指标；列 = 区间内的每一天，列头逐日标注日期（MM-DD）；
     - 单元格 = 该 SPU 该指标当天的值；当天无数据则显示 `0`（灰色字表示该项原本缺失，按 0 计入）；
     - 矩阵可**横向滚动**，所有 SPU 的日期列对齐。

> 单日模式是区间模式的「只有 1 天列」特例；二者由所选日期范围自动决定，无需手动切换。

### 5.2 单品分析页 `/module/:moduleId/spu/:spu`（路由名 `spu-analysis`）

- 顶部：返回列表 + 统计区间（同范围选择器，可自定义时间范围，后端重新聚合）+ 指标配置。
- **默认统计区间 = 最近一周**：以数据最新日为终点往前 7 天（含最新日），例如最新日 `2026-09-17` → 默认 `2026-09-10 ~ 2026-09-17`；
  数据不足一周时被最早日期截断。手动改过区间后不再自动套回默认（切换 SPU 也保留当前选择），
  可用日历旁的 **近一周** 快捷按钮一键还原；按钮组的 **当月 / 上月** 与模块页一致。
- **指标卡**：所选区间的汇总值（比率类按口径重算）。
- **趋势图（固定 3 张，含 2 张双轴合并）**：
  - ① **成交转化率 & 商品访客数**：双轴——左轴「商品访客数」(数值)、右轴「成交转化率」(百分比)。
  - ② **成交金额 & 搜索点击率**：双轴——左轴「成交金额」(金额)、右轴「搜索点击率」(百分比)。
  - ③ **推广占比**：单轴百分比，带 **可手动调整的警戒线**（页面输入阈值百分比，实时生效）。
  - 百分比率指标的 y 轴刻度标签统一以 **百分格式** 展示（如 `6.6%`）；tooltip 按各序列自身格式显示。
- **工作日 vs 节假日（纵向条形图）**：固定 4 项——日均成交金额 / 成交转化率 / 推广占比 / ROI。
- **综合分析**：自动生成的文字结论（含推广表现段落）。
- 法定节假日表在 `backend/app/analysis.py` 顶部，**每年需维护**。

### 5.3 指标配置（⚙）

- 点击右上角「⚙ 指标配置」勾选要展示的指标。
- 勾选结果存 `localStorage`（key `jd.metric-config.v1`，**按 moduleId 分别存储**），
  **模块页与单品分析页共用同一份配置，天然同步**。
- 指标清单的**唯一事实来源**是 `frontend/src/metrics.ts` 的 `METRICS`：
  新增一个指标只需在该文件加一条 spec，并同步后端 `models.py` / `transforms.py` / `api.py` 的字段，
  页面卡片、表格列、趋势图、对比行会自动派生，无需在视图里手写累加逻辑。

**配置面板的分组与指标（共 15 项，分 3 组）**

| 分组 | 指标 |
| --- | --- |
| 商品明细表 | 成交金额、成交单量、成交客户数、成交商品件数、成交转化率、客单价、搜索曝光次数、搜索点击率、商品访客数 |
| 推广数据表 | 推广花费、推广成交金额、ROI、推广占比 |
| 退款 / 其他 | 取消及售后退款金额、取消及售后退款单量 |

- 默认勾选 8 项：商品明细表 4 项（成交金额、成交客户数、成交转化率、商品访客数）+ 推广数据表 4 项（推广花费、推广成交金额、ROI、推广占比）；
  成交单量、成交商品件数、客单价、搜索曝光次数、搜索点击率、取消及售后退款金额/单量默认不勾选。
- 原「模块概览 / 总SPU数」分组已从配置面板移除（总SPU数此前也仅作全局计数、不进入卡片/矩阵；页面顶部 SPU 数仍由数据汇总 `manifest.spu_count` 显示，与指标配置无关，始终可见）。

---

## 6. 接口速查

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/api/manifest` | 模块清单（含各模块 shops / date_range / spu_count） |
| GET | `/api/module/{id}/rows` | 模块分页明细。**支持 `start`+`end` 区间**（优先级高于兼容字段 `date`）；返回每个 SPU 的 `days` 逐日数组（区间内每天一列，缺失日指标为 null） |
| GET | `/api/module/{id}/summary` | 模块汇总（店铺×日期趋势、TOP SPU、店铺汇总） |
| GET | `/api/module/{id}/spu/{spu}/analysis` | 单品分析（趋势 / 工作日节假日 / 洞察）。支持 `start`+`end` 区间 |
| GET | `/api/status` | 上次批处理状态 |
| POST | `/api/refresh` | 手动触发重跑批处理（同步，完成后清缓存） |

`/rows` 的 `days` 结构（前端据此渲染网格 / 矩阵）：

```jsonc
{
  "shop": "店铺A", "spu": "100...", "spu_name": "...", "category": "...", "image": "...",
  "date": "2026-09-17",          // 单日模式=该日；区间模式为空串
  "metrics": { /* 单日模式=days[0]，区间模式全 null */ },
  "days": [                      // 区间模式核心：该 SPU 在所选日期范围内逐日数据
    { "date": "2026-09-17", "metrics": { "amount": 1520.1, ... } },
    { "date": "2026-09-16", "metrics": { "amount": null, ... } }  // 缺失日指标为 null
  ]
}
```

---

## 7. 维护注意事项

- **改前端**：`npm run build`（dist 即 FastAPI 实际托管产物）。
- **改后端 / 数据源**：重跑 `run_batch` 并**重启 uvicorn 8000**（旧进程会返回旧字段，表现为「改了没生效」）。
- 数据源目录里的 Excel 打开中会产生 `~$*.xlsx` 锁文件，批处理已过滤，新增数据源时沿用此过滤。
- 单品分析页的法定节假日表每年维护（`backend/app/analysis.py` 顶部）。
- 指标口径变更务必同时维护 `frontend/src/metrics.ts` 与 `backend/jobs/transforms.py`，保持前后端一致。
